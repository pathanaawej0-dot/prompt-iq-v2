import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "PromptIQ - Transform Ideas Into Legendary Prompts",
  description: "The #1 AI-Powered Prompt Engineering Platform for Creators, Founders & Professionals. Generate professional, detailed prompts instantly.",
  keywords: ["AI", "prompts", "prompt engineering", "ChatGPT", "Claude", "AI tools"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
        <Toaster />
      </body>
    </html>
  );
}
